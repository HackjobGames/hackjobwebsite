{
  "name": "HackjobGamesWebServer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "test",
    "dev": "bash build.sh -w",
    "build-server": "cd ./src/server && tsc --project ./tsconfig.json",
    "build-client": "cd ./src/client && npm run build",
    "migrate": "npx prisma migrate deploy",
    "start": "node -r dotenv/config -r esm ./dist/server/index.js",
    "serve": "npm run build-server && npm run start",
    "hello": "echo hello"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "./src/client",
    "./src/server"
  ],
  "dependencies": {
    "@prisma/client": "^4.9.0",
    "prisma": "^4.9.0"
  },
  "engines" : { 
    "node" : ">=16.0.0 <17.0.0"
  }
}
